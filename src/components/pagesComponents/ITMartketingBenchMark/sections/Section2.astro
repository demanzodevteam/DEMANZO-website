---
import { API_URL } from "../../../../config/urls";
import HeroSection from "../../../common/HeroSection";

const res = await fetch(API_URL + 'blocks/200');
const pageData = await res.json();

const bgImg= true
const sectionNumber = 2;
const combinedData ={
    ...pageData[1],
    bgImg,sectionNumber
}
---
<!-- section 2 -->

<HeroSection pageData={combinedData} />

