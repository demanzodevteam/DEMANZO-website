---
import { API_URL, BLOGS } from "../../../../config/urls";
import { MEDIA_URL } from "../../../../config/urls";
import RecentBlogSection from "../../../common/RecentBlogSection";


// const res = await fetch(API_URL + "blocks/204");
// const data = await res.json();

// const pageData = data[12];
// const list = pageData?.list_items[0];

  const res = await fetch(BLOGS);
  const blogs = await res.json();
const blogArray = Object.values(blogs);
---

<RecentBlogSection pageData={blogArray} client:load/>
