---
import { API_URL } from "../../../../config/urls";

const res = await fetch(API_URL + "blocks/204");
const data = await res.json();

const pageData = data[11];
import PartnerLocation from "../../../common/PartnerLocation";
---

<PartnerLocation pageData={pageData} />