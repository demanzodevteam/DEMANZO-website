---
import { API_URL } from "../../../../config/urls";
import RequestDemoForm from "../../../common/RequestDemoForm";
import SalesContactSection from "../../../common/SalesContactSection";

const slug = "189"; 

const res = await fetch(`${API_URL}blocks/${slug}`);
const pageData = await res.json();
---

<!-- Section 5 -->

<SalesContactSection  SectionData={pageData[4]} client:only="react">
     <RequestDemoForm client:load/>
</SalesContactSection>