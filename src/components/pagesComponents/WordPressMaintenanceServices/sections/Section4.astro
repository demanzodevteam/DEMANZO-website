---
import meetingImg from '../../../../assets/meeting-2.webp';
import { API_URL } from '../../../../config/urls';
import WhyMarketing from '../../../common/WhyMarketing';

const slug = "189"; 

const res = await fetch(`${API_URL}blocks/${slug}`);
const pageData = await res.json();
---

<!-- Section 4 -->
<WhyMarketing pageData ={pageData[3] } client:load/>