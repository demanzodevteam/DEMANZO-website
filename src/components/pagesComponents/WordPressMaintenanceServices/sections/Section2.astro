---
import { API_URL } from "../../../../config/urls";
import FiveFeatureSection from "../../../common/FiveFeaturesSection";

// Static or dynamic slug
const slug = "189"; 

const res = await fetch(`${API_URL}blocks/${slug}`);
const data = await res.json();
const pageData = data[1];
---
<!-- Section Three -->

<FiveFeatureSection pageData={pageData} />





