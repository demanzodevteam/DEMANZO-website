---
import { API_URL } from '../../../../config/urls';
import DetailedListing from '../../../common/DetailedListing.jsx';

const res = await fetch(API_URL + 'blocks/842');
const pageData = await res.json();

const background = 'bg-[#FFFFF]';
const colVal = [
  { value: 2 },
  { value: 2 }, 
  { value: 3 }
];
const noPoints = 'list-disc pl-0'
const combinedData = {
    ...pageData[3],
    background,colVal,noPoints
  };
---
<!------ Sixth Section ------>

<DetailedListing pageData={combinedData} client:load />

